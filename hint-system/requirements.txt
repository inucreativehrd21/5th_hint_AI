# ============================================================================
# INSTALLATION ORDER (IMPORTANT!)
# ============================================================================
# 1. pip install --upgrade pip
# 2. pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cu121
# 3. pip install -r requirements.txt
# 4. pip install vllm==0.6.3 --no-cache-dir
# 5. pip uninstall pyairports -y && pip install pyairports==2.1.3
# ============================================================================

# UI Framework
gradio==4.44.0

# Core dependencies (순차 설치 필수 - PyTorch 먼저!)
transformers==4.45.0
# torch==2.1.2  # 별도로 먼저 설치: pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cu121
accelerate==0.27.2
sentencepiece==0.2.0
protobuf==4.25.3

# ============================================================================
# vLLM Dependencies (vLLM 설치 전에 필요)
# ============================================================================
# pyairports - vLLM의 outlines가 사용 (최신 버전 필수!)
# vLLM 설치 후 반드시 재설치: pip uninstall pyairports -y && pip install pyairports==2.1.3
# pyairports==2.1.3  # vLLM 설치 후 별도 설치 필요

# ============================================================================
# vLLM - 별도 설치 (requirements.txt 설치 후!)
# ============================================================================
# vLLM은 requirements.txt 설치 후에 따로 설치하세요
# pip install vllm==0.6.3 --no-cache-dir
# 설치 후: pip uninstall pyairports -y && pip install pyairports==2.1.3
# vllm==0.6.3  # 주석 처리 - 별도 설치 권장

# Quantization support for HuggingFace models (4-bit/8-bit)
bitsandbytes==0.44.1  # For 4-bit quantization of large models

# ============================================================================
# Model Inference Backends
# ============================================================================

# OpenAI API 호환 (vLLM 서버 클라이언트 - REQUIRED for vLLM)
openai==1.54.0

# Ollama - 로컬 환경용 (선택 사항)
# ollama>=0.1.0  # Uncomment if using Ollama

# ============================================================================
# Utilities
# ============================================================================
tqdm==4.66.5
pandas==2.2.3
numpy==1.26.4

# Configuration
pyyaml==6.0.2
python-dotenv==1.0.1
requests==2.32.3
