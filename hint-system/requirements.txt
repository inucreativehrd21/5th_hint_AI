# ============================================================================
# INSTALLATION ORDER (IMPORTANT!)
# ============================================================================
# 자동 설치: bash install_dependencies.sh
# 
# 수동 설치:
# 1. pip install --upgrade pip tomlkit
# 2. pip install vllm==0.6.3 --no-cache-dir
# 3. pip uninstall pyairports -y && pip install airportsdata
# 4. pip install -r requirements.txt
# ============================================================================

# UI Framework
gradio==4.44.0

# Core dependencies (vLLM이 torch 2.4.0 설치함)
transformers==4.45.0
# torch는 vLLM이 자동으로 설치 (2.4.0)
accelerate==0.27.2
sentencepiece==0.2.0
protobuf==4.25.3

# ============================================================================
# vLLM Dependencies Fix
# ============================================================================
# vLLM은 별도 설치: pip install vllm==0.6.3
# pyairports 0.0.1 문제 해결: pip install airportsdata
# airportsdata는 install_dependencies.sh에서 자동 설치됨

# Quantization support
bitsandbytes==0.44.1

# Dependency conflict fixes
tomlkit>=0.12.2  # runpod 요구사항

# ============================================================================
# Model Inference Backends
# ============================================================================

# OpenAI API 호환 (vLLM 서버 클라이언트 - REQUIRED for vLLM)
openai==1.54.0

# Ollama - 로컬 환경용 (선택 사항)
# ollama>=0.1.0  # Uncomment if using Ollama

# ============================================================================
# Utilities
# ============================================================================
tqdm==4.66.5
pandas==2.2.3
numpy==1.26.4

# Configuration
pyyaml==6.0.2
python-dotenv==1.0.1
requests==2.32.3
