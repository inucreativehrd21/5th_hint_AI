#!/bin/bash
# ============================================================================
# 스크립트 실행 권한 설정
# ============================================================================

echo "🔧 스크립트 실행 권한 설정 중..."

# 현재 디렉토리의 모든 .sh 파일에 실행 권한 부여
chmod +x *.sh

echo "✅ 완료! 다음 스크립트들에 실행 권한이 부여되었습니다:"
ls -lh *.sh | awk '{print "   " $9 " (" $1 ")"}'

echo ""
echo "📝 사용 가능한 명령어:"
echo "   bash runpod_start.sh      # 서버 시작"
echo "   bash runpod_stop.sh       # 서버 중지"
echo "   bash runpod_restart.sh    # 서버 재시작"
echo "   bash runpod_status.sh     # 상태 확인"
echo ""
