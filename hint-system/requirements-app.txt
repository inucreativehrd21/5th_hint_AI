# ============================================================================
# Gradio 힌트 생성 앱용 경량 Requirements
# vLLM 서버는 별도 Docker 이미지로 실행되므로, 여기서는 클라이언트만 설치
# ============================================================================

# UI Framework
gradio==4.44.0

# OpenAI API 호환 클라이언트 (vLLM 서버와 통신)
openai>=1.3.0,<2.0.0

# Configuration
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0
